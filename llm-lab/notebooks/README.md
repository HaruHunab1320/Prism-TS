# Notebooks

Drop exploration notebooks here (Jupyter, VS Code `.ipynb`, etc.). Common topics:

- Inspecting Prism code distributions
- Visualizing confidence metadata
- Prototyping evaluation metrics
- Prompt engineering experiments

If notebooks depend on specific Python packages, document them in a `requirements.txt` or `environment.yml` next to the notebook.
