---
"@prism-lang/core": minor
"@prism-lang/validator": minor
---

Added runtime module resolution system with import/export support, confident ternary operator (~?), and confident assignment operators (~+=, ~-=, ~*=, ~/=). Updated validator to support new operators.