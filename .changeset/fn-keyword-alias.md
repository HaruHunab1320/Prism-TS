---
"@prism-lang/core": major
"@prism-lang/validator": major
---

Add `fn` as a reserved keyword alias for `function` in function declarations. This is a breaking change — any code using `fn` as a variable or identifier name will need to be renamed.
