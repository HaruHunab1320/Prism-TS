# Lumina Multi-Model (Clean Slate)

This directory is the clean-slate workspace for the multi-model (router + specialists + aggregator) architecture.

Planned structure (to be filled):

```
lumina_multimodel/
├── data/
├── datasets/
├── models/
├── training/
├── inference/
├── evaluation/
├── outputs/
└── run_multimodel.py
```

Notes:
- We kept the original generated datasets in `lumina_basic/` for now. If you want them removed, say the word and I'll delete them.
- We should add router/aggregator training scripts here and a unified evaluation harness.
