# `js_filter_predicate_refactor` Flow

1. User asks to refactor a simple conditional-push loop.
2. Router checks for the narrow filter loop shape.
3. Specialist returns one filter assignment.
4. Verifier checks syntax, `.filter(...)`, and behavior tests.
5. If verification fails, runtime falls back.
